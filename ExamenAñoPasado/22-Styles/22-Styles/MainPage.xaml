<Page
    x:Class="_22_Styles.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_22_Styles"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style TargetType="Button" x:Key="Resource Page" >
            <Setter Property="Control.FontFamily" Value="Times New Roman" />
            <Setter Property="Control.FontSize" Value="18" />
            <Setter Property="Control.FontWeight" Value="Bold" />
            <Setter Property="Control.Foreground" Value="Red"/>
        </Style>

       
    </Page.Resources>

            <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


        <Grid.RowDefinitions>
            <RowDefinition Height="AUTO"/>
            <RowDefinition Height="AUTO"/>
            <RowDefinition Height="AUTO"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Margin="10" Style="{StaticResource Resource Page}" Content="Boton estilo generico"></Button>
        <Button Grid.Row="1" Margin="10" Content="Boton estilo personalizado2 Con Diccionario" Style="{StaticResource StyleButton2}"></Button>
        <Button Grid.Row="2" Margin="10" Content="Boton Sin Estilo"></Button>
    </Grid>
</Page>
